#pragma once
#include <string>
const double PI = 3.14; // in a const variable declaration we must need to define the variable with extern, because by default const has internal linkage
std::string cppName;